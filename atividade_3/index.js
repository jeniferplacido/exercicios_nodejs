let rs = require ("readline-sync")

let quad = rs.question ("Seja Bem Vindo a Arbyte Calculos, qual o tamanho dos lados do quadrado?")

let resultado = (quad*quad)

console.log ("O resultado da area do quadrado é:" +resultado)

